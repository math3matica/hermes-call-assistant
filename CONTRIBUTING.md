# Contributing

Keep the plugin boundary explicit, preserve protocol/runtime compatibility, and add tests for every behavior change. Do not commit credentials, private data, generated learning state, model artifacts, APKs, or device output. Run the repository's tests and `python3 -m compileall` before proposing changes.

A license has not yet been selected by the project owner. Choose and add one before public publication.
